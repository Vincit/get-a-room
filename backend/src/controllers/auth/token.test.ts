describe('token', () => {
    describe('createToken', () => {
        test.todo('Should set JWT token to locals');
    });

    describe('updateToken', () => {
        test.todo('Should update the accessToken field of JWT');
    });

    describe('readToken', () => {
        test.todo('Should throw exception when invalid secret is given');
        test.todo('Should return JWT payload');
    });

    describe('writeToken', () => {
        test.todo('Should return signed JWT token');
    });
});
